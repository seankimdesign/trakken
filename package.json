{
  "name": "process-tracker",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d dist",
    "fix": "standard --fix --silent",
    "start": "npm run fix && npm run build && node dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "dependencies": {
    "lodash": "^4.17.10",
    "ps-node": "^0.1.6",
    "sqlite3": "^4.0.1",
    "tasklist": "^3.1.0"
  }
}
